metadata:
  labels:
    agent-type: CodeQuality
spec:
  containers:
    - name: jnlp
      env:
        - name: CONTAINER_ENV_VAR
          value: jnlp
      securityContext:
        runAsUser: 1000

    - name: gradle-container
      image: 'gradle:jdk11'
      command:
        - cat
      tty: true
      securityContext:
        runAsUser: 1000

